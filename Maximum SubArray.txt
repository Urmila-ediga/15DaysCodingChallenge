class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        r=nums[0]
        ms=0
        for i in nums:
            if ms<0:
                ms=0
            ms+=i
            r=max(r,ms)
        return r
    
